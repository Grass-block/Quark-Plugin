plugin-vpn:
  enabled: false                         #if the service is enabled.
  address: "127.0.0.1"                   #default address for exchange/endpoint. if its exchange it should be 127.0.0.1.
  port: 25830                            #the port of exchange server start/connect.
  exchange: false                        #endpoint('client')/exchange('router').
  identifier: plugin-pvn-example-server  #unique id in network.
  secret: "PLUGIN_VPN_DEFAULT_SECRET"    #key.
